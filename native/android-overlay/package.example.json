{
  "_comment": "Example dependencies for the native Capacitor build. Merge these into the package.json that `npm init` / `npx cap init` creates at the repo root. Versions are indicative — run `npm install @capacitor/core@latest` etc. to pin current ones. This file is documentation only and is NOT used by the web PWA.",
  "name": "azkar",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "cap:sync": "cap sync android",
    "cap:open": "cap open android",
    "cap:run": "cap run android"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
